{
    let amount = 2;
    amount = amount * 3;
    {
        let amount = 4;
        console.log(amount);
    }
    console.log(amount);
}

// == checks for equality with coercion allowed and === checks for equality without coercion allowed.
// (void 0) == undefined
